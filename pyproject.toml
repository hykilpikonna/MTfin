[project]
name = "trackertracker"
version = "0.1.0"
description = "Something to track your torrent trackers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bencodepy>=0.9.5",
    "openai>=2.26.0",
    "playwright",
    "pyotp",
    "qbittorrent-api>=2025.11.1",
]
